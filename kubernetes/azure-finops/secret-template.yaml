apiVersion: v1
kind: Secret
metadata:
  name: azure-finops-credentials
  namespace: azure-finops
type: Opaque
stringData:
  # Replace with your Azure Service Principal credentials
  # The SP needs "Cost Management Reader" role on the subscription
  AZURE_SUBSCRIPTION_ID: "REPLACE_ME"
  AZURE_TENANT_ID: "REPLACE_ME"
  AZURE_CLIENT_ID: "REPLACE_ME"
  AZURE_CLIENT_SECRET: "REPLACE_ME"
