---
apiVersion: v1
kind: Service
metadata:
  name: handbrake
  namespace: handbrake
spec:
  selector:
    app: handbrake
  ports:
  - name: http
    port: 5800
    targetPort: 5800
    protocol: TCP
  type: ClusterIP
