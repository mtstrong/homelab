# Falco (Argo CD + Helm)

This folder provides Helm values for Falco, deployed via Argo CD.

## Notes
- The chart is sourced from the Falco community Helm repository.
- Update values in values.yaml as needed for your kernel/driver setup and alerting.
