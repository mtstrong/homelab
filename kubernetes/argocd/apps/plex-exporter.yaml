apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: plex-exporter
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/mtstrong/homelab.git
    targetRevision: main
    path: kubernetes/plex-exporter
  destination:
    server: https://kubernetes.default.svc
    namespace: plex-exporter
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
    - CreateNamespace=true
