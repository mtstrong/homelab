apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: tinyauth
  namespace: argocd
  labels:
    app.kubernetes.io/part-of: homelab
    app.kubernetes.io/name: tinyauth
spec:
  project: default
  source:
    repoURL: https://github.com/mtstrong/homelab.git
    path: kubernetes/tinyauth
    targetRevision: HEAD
  destination:
    server: https://kubernetes.default.svc
    namespace: tinyauth
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
    - CreateNamespace=true
