apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-dashboard-media-server
  namespace: grafana
  labels:
    grafana_dashboard: "1"
data:
  media-server.json: |
$(cat /tmp/media-server.json | sed 's/^/    /')
