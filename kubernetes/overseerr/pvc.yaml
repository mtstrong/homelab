apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: overseerr
  namespace: overseerr
spec:
  accessModes:
  - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 1Gi