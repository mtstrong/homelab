# Uptime Kuma on OCI (Ansible)

This playbook installs Docker and runs Uptime Kuma with persistent data in /opt/uptime-kuma/data.

Prerequisites on your control machine:
- Ansible 2.14+
- community.docker collection

The inventory file is generated by Terraform at ansible/uptimekuma-oracle/inventory.ini.
